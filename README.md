# Vision-Agent---Multi-Modal-Receipt-Product-Analyzer
A tool-using LLM agent that combines computer vision (OCR + object detection) with an LLM that decides, on its own, which tool to call and when - receipts get parsed, verified, and fact-checked; product photos get identified.
